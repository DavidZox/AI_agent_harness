# stt_engine 經驗記憶

> 使用者要求記住、專屬於此技能的經驗。載入 `stt_engine` 規格文件時會自動附在後面；由 `scripts/modify_memory_cmd.py "..." --skill stt_engine` 寫入，可直接編輯或刪除過時條目。

- [09-19 16:29] 錯誤執行 | 語音輸入 (stt_engine) 失敗 | 缺少 Python 模組 'faster-whisper'，需在環境中安裝此依賴。
