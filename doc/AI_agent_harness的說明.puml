@startuml
' --- 樣式精準控制 ---
skinparam RectangleWidthMax 340
skinparam ranksep 60
skinparam nodesep 60
skinparam shadowing false
skinparam defaultFontName "Microsoft JhengHei"

title AI_agent_harness 架構：一個主 session 負責「挑技能」，一次性獨立 session 負責「讀原文」

rectangle NodeUser #F5F5F5 [
  **使用者介面**
  CLI：Agent_Runner.py 的 main()
  Web：web_console.py（重用 SkillAgent，不重寫規則）
  ----
  * 輸入：自然語言任務、slash 指令、附圖、/skill 手動載入規格
  * 顯示：reply；每次工具回傳兩張卡——完整原文（#編號）
    與「AI 實際收到的內容」；決策／Plan／make_skill 按鈕
]

rectangle NodeAgent #D1E5F0 [
  **SkillAgent（Agent_Runner.py，唯一核心）**
  ----
  * 每輪重組 system prompt（尾端附工具使用檢索清單）
  * 呼叫主 session，**只看 action** 派發
  * 執行腳本 → 記軌跡 → 原文存檔 #編號
  * 決定工具回傳怎麼進主對話（原文／摘要／成功失敗）
  * 替模型維護狀態：工作目錄、目標容器、任務線、計畫
  * 上下文超過水位就壓成滾動摘要
]

rectangle NodeLLM #FFE6CC [
  **主 session（Ollama gemma4:e4b，有對話歷史）**
  ----
  每輪固定回 JSON {thought, reply, action}
  action 只能是：技能名稱、規格標明的腳本路徑、
  或 result_recall；reply 裡的文字不會被執行
]

rectangle NodeSub #FCE5CD [
  **一次性獨立 session（不碰主對話、做完即丟）**
  ----
  * 工具摘要 summarize_tool_result：>500 tokens 的回傳
  * 回原文提煉 result_recall：main session 主動選
    （兩者共用 _extract_task_oriented：回答／事實／未涵蓋／
     延伸方向／index_hint／related_records ＋下一步建議）
  * 滾動壓縮 summarize_messages：舊對話 → 融合摘要
  * 視覺 sub-session：附圖 → 文字
  * /make_skill 草擬：操作軌跡 → 技能草稿 JSON
]

rectangle NodeSkills #D5E8D4 [
  **skills_system/（模型能做的事只有這些）**
  ----
  * SKILLS.md：一行一個技能（常駐 system prompt）
  * tools/<name>.md：規格（按需載入，附技能經驗記憶）
  * scripts/<name>_cmd.py：真正執行的腳本
  * 檔案系統／容器與 ROS2／調度系統／存檔回查／記憶
  * 慣例：stdout 以 [PASS]／[ERROR] 開頭；計數、排序、門檻由腳本算
]

rectangle NodeWorld #FFF2CC [
  **被操作的環境**
  ----
  宿主機檔案系統、Docker 容器與容器內 ROS2、
  fih_rmf_system web_console（:8020）
]

rectangle NodeStore #E1D5E7 [
  **logs/ 與記憶（logs/ 已 .gitignore）**
  ----
  * tool_results/<session>_<編號>_<腳本>.md：每次執行的完整原文
    （編號跨 session 唯一；留最近 200 檔／50 MB）＋ index.md
  * tool_results/tools_use_index.md：大量回傳的一句話關聯描述
    （「#編號｜使用者想知道什麼：原文有什麼」，永久累加）
  * trajectory.jsonl（操作軌跡）、summary_*.md（滾動摘要歸檔）
  * Memory.md（全域常駐）、skills_system/memory/<name>.md（綁技能）
]

NodeUser -down-> NodeAgent : 任務／slash／附圖
NodeAgent -up-> NodeUser : reply、原文卡片、\nAI 實際收到的內容
NodeAgent -right-> NodeLLM : system prompt + 對話歷史
NodeLLM -left-> NodeAgent : {thought, reply, action}
NodeAgent -[#996633]right-> NodeSub : 原文 + 使用者問題 +\n檢索清單／舊對話／影像／軌跡
NodeSub -[#996633]left-> NodeAgent : 重點 + 下一步建議／\n新摘要／影像文字／草稿
NodeAgent -down-> NodeSkills : 技能名稱 → 回規格文件（不執行）
NodeAgent -[#CC6600,bold]down-> NodeSkills : 腳本路徑 → subprocess
NodeSkills -down-> NodeWorld : docker exec／HTTP／檔案操作
NodeWorld -up-> NodeSkills : 原始輸出
NodeSkills -[#009933,bold]up-> NodeAgent : [PASS]／[ERROR] 完整輸出\n+ [CWD_CHANGED]／[TARGET_CONTAINER]
NodeAgent -left-> NodeStore : 存檔原文、檢索描述、軌跡、摘要歸檔
NodeStore -right-> NodeAgent : 檢索清單、最新摘要、常駐記憶；\nrecall 讀回原文
NodeLLM -[hidden]down-> NodeSub

note right of NodeLLM
  **兩階段按需載入：**
  沒有「技能名稱 → 腳本」對照表。
  填技能名稱只會拿到規格書，
  讀到規格裡的真實腳本路徑
  才執行得了——由程式保證。
end note

note bottom of NodeWorld
  **安全邊界：**模型不產生程式碼，
  能做的只有既有腳本；Plan 模式的
  核准關卡靠「規劃階段不呼叫
  run_tool」保證。
end note

@enduml
