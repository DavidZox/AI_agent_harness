@startuml
' --- 樣式精準控制 ---
skinparam RectangleWidthMax 370
skinparam ranksep 45
skinparam nodesep 55
skinparam shadowing false
skinparam defaultFontName "Microsoft JhengHei"

title AI_agent_harness 一回合的狀態機：從使用者訊息到使用者看到結果（含工具回傳的每一條路）

rectangle State1 #D1E5F0 [
  **1. Input（收到使用者訊息）**
  * slash 指令（/auto、/summarize、/plan on、/skill…）
    由 harness 直接處理，不進模型、不算回合。
  * 文字任務：set_current_task → 任務線 task_history
    （最近 3 則），上一個已核准的計畫自動清除。
  * 附圖：先由獨立視覺 session 產生文字，
    以 [vision result] 附在訊息後（主對話永遠純文字）。
  * /skill 手動載入：規格以 [skill loaded] 附在訊息後。
  * /plan on 時改走規劃流程（右側）。
]

rectangle State2 #F5F5F5 [
  **2. Prompt（組 system prompt、硬水位檢查）**
  * AGENT.md + SKILLS.md 索引 + Memory.md
    + 最新滾動摘要 + Current Agent State
    （工作目錄、目標容器、Objective、已核准計畫）。
  * 上下文超過硬水位（NUM_CTX×75%）
    → 先同步壓縮（滾動壓縮 session）再呼叫模型。
]

rectangle State3 #FFE6CC [
  **3. Reply（主 session 回 JSON）**
  * {thought, reply, action}，Ollama format= 強制。
  * 非合法 JSON → 只顯示原文、本輪不執行。
  * 空白回覆（reply 空、action null）
    → 帶暫時提醒自動重試一次。
  * reply 顯示給使用者、thought 進右欄 。
]

rectangle State4 #FFF2CC [
  **4. Dispatch（只看 action）**
  * null → 回合結束（→ 7）。
  * 技能名稱（tools/<name>.md 存在）→ 規格文件
    + 經驗記憶當「工具回傳」（不執行、不存檔、
    不受門檻限制）→ 6c 直接進上下文 → 回 3。
  * 腳本路徑 → 執行（→ 5）。
  * 找不到腳本 → [ERROR] 指引先載規格（不存檔）→ 6c → 回 3。
]

rectangle State5 #D5E8D4 [
  **5. Execute（subprocess 執行腳本）**
  * cwd＝工作目錄；env CONTAINER_CWD、TARGET_CONTAINER、
    TOOL_RESULTS_DIR、HARNESS_SESSION。
  * 腳本自帶逾時；harness 600 秒最後防線；
    非零 exit code 補 [ERROR]、沒輸出給明確訊息。
  * 數值在這裡算：list_dir 計數／最新修改／~--filter／~--newest、
    topic／node list ~--filter 計數、echo ~--duration 統計 +
    全部原文 + ~--where 門檻判斷、status ~--watch 變化清單。
  * result_list／grep／view 讀 logs/tool_results/，不碰外部系統。
]

rectangle State6a #FFE0B2 [
  **6a. 落地（每次執行都做，與大小無關）**
  * 同步狀態標記 [CWD_CHANGED]／[TARGET_CONTAINER]。
  * 記一筆操作軌跡（logs/trajectory.jsonl，含 result_file）。
  * 完整原文存成結果檔 #id（logs/tool_results/<session>_<id>_<腳本>.md）
    + index.md 一行；#id＝軌跡 id。
  * 完整原文顯示給使用者：CLI 印出「已存檔 #id」；
    Web 卡片標題帶 #id、可開 /api/results/<id>；超過門檻標 。
]

rectangle State6b #FFD580 [
  **6b. 決定進主對話的內容（_content_for_context）**
  * (A) 規格文件、或 ≤500 tokens → 原文原封不動。
  * (B) >500 tokens 且 /summarize on（預設）→ 工具摘要 session：
    完整輸出（>16000 字只讀頭尾並註明）+ 使用者目標
    （Objective／任務線／計畫）+ 這一步的目的（thought／reply／action）
    → 執行結果／回答／相關事實（名稱數值照抄、腳本算好的數字為準）／
    錯誤／未涵蓋／可追問（最多 3 個＋關鍵字）+ 附註
    「存檔 #id，細節用 result_grep，不要重跑」；回答回填 index。
  * (C) /summarize off，或 (B) 失敗 → 只給「指令已成功／失敗執行」。
]

rectangle State6c #FFCC80 [
  **6c. 包裝與模式決策**
  * 內容包成 [tool result] + 框架句【系統回傳】
    「系統執行你上一輪 action（腳本）的結果，不是使用者提供的，
    不要感謝使用者」，以 user 角色加入主對話（gemma4 的 tool 角色會被丟掉）。
  * auto：直接加入、續推下一輪（Web 最多 25 輪）。
  * hybrid：問使用者 y／n；n 改加入「已捨棄」通知；都續推。
  * manual：y 加入並續推；n 捨棄後結束；stop 直接結束。
]

rectangle State7 #E1D5E7 [
  **7. End of turn（回合結束）**
  * 使用者在主對話看到最終 reply。
  * 上下文超過軟水位（NUM_CTX×60%）且可壓內容夠多
    → 滾動壓縮 session：舊訊息 + 上一份摘要 → 新摘要、
    歸檔 logs/summary_*（/parallel_cal on 時在背景執行緒）。
  * 壓縮渲染時工具結果保留內容、去掉框架句。
]

rectangle State8 #DDEBF7 [
  **8. 追問回查（下一回合，不重跑工具）**
  * 使用者說關鍵字（或選了可追問）→ 進 1；
    任務線讓摘要 session 仍知道原本的目的。
  * 模型改用 result_list／result_grep <#id> <關鍵字|同義詞>
    （-C 前後行、~--block 整則）／result_view <#id> ~--from 行號，
    照樣先載規格再執行（→ 4 → 5）。
  * grep 輸出走同一條 6：≤500 原文直接進主對話，
    >500 交摘要 session、這時的問題就是使用者的追問。
]

rectangle StatePlan #F8CECC [
  **Plan 模式（/plan on 後的下一個新任務）**
  * 模型只產出步驟清單，harness 不呼叫 run_tool。
  * y 核准 → 計畫注入 system prompt、自動退出
    Plan 模式、軌跡記起點 → 進入 2 執行。
  * n 取消／其他文字＝修改意見重新規劃。
]

rectangle StateSkill #ECECEC [
  **/make_skill（回合之外）**
  * 取上一個起點之後的成功步驟與失敗嘗試，
    草擬 session 只填 JSON（參數化、目的、注意事項）。
  * harness 驗證、依範本產生規格／組合腳本／索引行，
    使用者核准（可先重播驗證）才註冊。
]

' --- 邏輯流向 ---
State1 -down-> State2
State2 -down-> State3
State3 -down-> State4
State4 -[#CC6600,bold]down-> State5 : **腳本路徑**
State4 -[#3366CC]right-> State6c : 技能名稱：規格文件／\n找不到腳本的 [ERROR]（不執行）
State5 -down-> State6a
State6a -down-> State6b
State6b -down-> State6c
State6c -[#009933,bold]up-> State3 : **結果加入上下文**\n（auto／y／hybrid）
State4 -[#CC0000,bold]left-> State7 : **action null**
State6c -[#CC0000]left-> State7 : manual 的 n／stop
State7 -[#6A5ACD,dashed]down-> State8 : 使用者追問細節
State8 -[#6A5ACD,dashed]up-> State1 : 新訊息（關鍵字）
State1 -right-> StatePlan : /plan on
StatePlan -[#009933,bold]down-> State2 : **核准**
State7 -[#999999]down-> StateSkill : 做對之後 /make_skill

note right of State4
  **狀態轉移單一來源：**
  只有 action 欄位驅動轉移；
  reply／thought 裡的指令文字、
  複述的規格、舉例都不算。
  這一點靠程式碼保證，
  不依賴模型自律。
end note

note left of State6a
  **狀態同步：**
  change_dir 印 [CWD_CHANGED]、
  容器技能印 [TARGET_CONTAINER]，
  harness 讀到就更新，下一輪的
  system prompt 與腳本 env 即生效；
  /clear 不清這兩個狀態，
  也不清 tool_results/。
end note

note left of State6b
  **獨立 session 都是一次性：**
  自己的 system／user prompt，
  不接觸主對話，做完即丟；
  同模型時與主對話序列執行
  （Ollama 單 slot，e4b 約 3～4 秒）。
end note

@enduml
