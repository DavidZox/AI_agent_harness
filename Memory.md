# Long Term Memory

[05-21 14:01] 操作流程規範 | 執行任何查看檔案內容的指令 (如 cat, view_file) | 必須先向使用者確認，禁止直接執行

[05-21 16:57] 操作流程 | 執行指令時需嚴格遵循使用者指定的步驟順序 | 必須分階段執行，每一步驟執行後等待系統回傳結果再進行下一步指令

[05-21 17:01] 溝通風格 | 開頭問候語 | 後續回答必須以「嗨使用者」開頭

[09-19 03:02] 錯誤執行 | 查詢技能規格時，系統持續誤判為執行腳本指令 | 應當在查詢規格時，僅輸出技能名稱，並等待系統回傳規格文件，而非觸發腳本執行。
[09-19 03:42] 操作流程規範 | 執行多步驟任務時 | 必須嚴格遵循一次推論一個指令的原則，每一步操作後必須等待系統回饋才能進行下一步。

[09-19 13:41] 操作流程規範 | 搜尋檔案名稱 | 每次協助使用者前，優先使用 find_file 技能搜尋使用者提供的關鍵字；若使用者未提供關鍵字，則主動詢問使用者希望搜尋的關鍵字

[09-19 13:58] 操作流程規範 | 工具回傳過長時的處理機制 | 系統回傳結果時，應僅照實說明成功或失敗狀態，並詢問後續期望行為，避免重複輸出全部內容。

[09-19 14:01] 當執行任何操作失敗時，應優先查閱相關的工具規格文件，而不是立即向使用者尋求幫助。

[09-19 14:17] 工具使用 | 使用者詢問技能表單時 | 應當直接回顯或總結 Available Skills (SKILLS.md) 的內容，無需執行任何指令。

[09-19 16:29] 錯誤執行 | 語音輸入 (stt_engine) 失敗 | 缺少 Python 模組 'faster-whisper'，需在環境中安裝此依賴。

[09-19 19:15] 操作流程規範 | 針對特定資料夾內的檔案操作 (如 view_file, search_text) | 必須先使用 change_dir 切換到該資料夾，再執行後續操作，以確保路徑正確性。

[09-22 13:38] 專案經驗 | AMR 系統 CPU 性能診斷流程 | 1. 獲取 CPU 報告 (如 AMR_CPU_usage_report)。2. 判斷峰值是否超過安全紅線 (如 85%)。3. 核心優化方向為：使用 taskset/chrt 進行關鍵節點的硬體核心隔離和 RT 優先權保護，確保即時控制環的確定性。

[09-22 17:48] 操作流程規範 | 參數缺失檢查 | 無論是在規劃模式還是執行模式，若任何指令或步驟需要參數但使用者未提供，必須主動停下來，向使用者確認所需的參數，直到使用者確認為止。

[09-22 23:34] 溝通風格 | 避免在非指令輸出的敘述性文字中突然出現 EXECUTE: | 除非是明確的指令輸出，否則不應在描述性文字中包含 EXECUTE: 關鍵字。

[09-23 00:09] 操作流程規範 | 查看路徑時的判斷邏輯 | 判斷使用者詢問的路徑是本機路徑還是容器內路徑；若為容器內路徑，必須使用 docker 相關技能進行操作。

[09-23 00:20] 操作流程規範 | EXECUTE: 關鍵字使用限制 | EXECUTE: 關鍵字僅應出現在單次回應的最後，且在整個回應中不得重複出現，需謹慎使用。

[09-23 00:28] 操作流程規範 | 路徑操作判斷 | 每次執行路徑相關指令前，必須判斷使用者意圖是操作本機環境還是容器環境。若為容器，則必須使用 docker_runcmd 或相關 docker 技能；若為本機，則使用本地目錄操作技能 (如 cd_cmd)。
